function on_message(self, message_id, message, sender)
	if message_id == hash("destroy_cards") then
		for _, go_id in pairs(message.card_ids) do
			print('trying to delete', go_id)
			
			if go.exists(go_id) then
				print('deleting', message.go_id)
				go.delete(go_id)
			end
		end
	end
end
